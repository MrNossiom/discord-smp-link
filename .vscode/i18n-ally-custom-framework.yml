languageIds: [rust]
enabledParsers: [ftl]

usageMatchRegex:
  # Translation Trait
  - "[^\\w\\d]*\\.get\\([\\n\\t ]*?['\"`]({key})['\"`]"
  # Applications commands functions
  - "#\\[command\\(slash_command.*\\)\\]\\npub\\(crate\\) async fn (\\w+)\\("

monopoly: true
